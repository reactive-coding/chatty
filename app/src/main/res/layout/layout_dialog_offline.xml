<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/dialog_width"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/dialog_title_margin_top">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingLeft="@dimen/dialog_content_padding"
        android:paddingRight="@dimen/dialog_content_padding"
        android:scrollbars="none">

        <TextView
            android:id="@+id/dialog_permission_message"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/dialog_textview_margin_bottom"
            android:layout_marginTop="@dimen/dialog_textview_margin_top"
            android:textColor="@color/color232326"
            android:textSize="18dp"
            android:includeFontPadding="false"/>
    </ScrollView>


    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="@dimen/dialog_button_content_height"
        android:gravity="center|bottom"
        android:orientation="horizontal">

        <Button
            android:id="@+id/dialog_permission_pos_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/button_dialog_pos"
            android:textColor="@color/colorF23030"
            android:textSize="16dp" />
    </LinearLayout>

</LinearLayout>